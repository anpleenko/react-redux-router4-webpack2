const initialState = {
  newUserSuccsess: false,
  newUserRequest: false,
  newUserFail: false,
  newUserId: null,
};

export const admin = (state = initialState, action) => {
  switch (action.type) {

    default:
      return state;
  }
};
